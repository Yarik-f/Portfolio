import styled from "styled-components";

const StyledSkills = styled.section`
`
const SkillsText = styled.p`
  font-size: 18px;
  margin-bottom: 80px;
`

export const S = {
    StyledSkills,
    SkillsText
}